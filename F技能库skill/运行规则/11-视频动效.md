# 11 · 视频动效 — 口播视频加动效层（overlay-fx-generator · vault 内调用入口）

> **触发**：用户说「给这期视频配动效 / 根据 SRT 生成动效 / 生成 overlay JSON / 加动效层 / 录完了要加动效」。
> **作用**：在 **vault 会话里**就能指挥仓库里的 overlay-fx-generator skill 干活，不用切工作目录。

## 前置事实（固定，别改）

- **仓库**：`/Users/mac/Documents/overlay-studio/`（2026-09-13 装好，Node/ffmpeg/Chrome 齐备）
- **技能真源**：`/Users/mac/Documents/overlay-studio/.claude/skills/overlay-fx-generator/SKILL.md`
  → ⛔ **规则不复制进 vault**。本文只写"怎么调"，**具体编排规则一律现读真源**，避免两边漂移
- **编辑台**：`cd /Users/mac/Documents/overlay-studio/motion-playground && npm run dev` → `http://localhost:5177`
- 详解档案 → [[总控台/自媒体实操zfc.md]] 三十五节

## 调用流程（照做）

1. **读真源**：`Read /Users/mac/Documents/overlay-studio/.claude/skills/overlay-fx-generator/SKILL.md`（28K，只读这一次，别重复读）
2. **读偏好表**（同目录，定义取值，**不许自己编数字**）：
   `我的偏好.default.md`（出厂默认）+ `我的偏好.md`（用户自己的，同名条目覆盖默认）
3. **要 SRT**：向用户索取字幕文件路径（剪映「识别字幕」导出，或 Whisper 转写）。
   没有 SRT → 先让用户去导，**不要凭空编时间戳**
4. **按真源流程产出**：解析 SRT → 先分段（章→段→元素）→ 识别内容类型 → 选卡 → 编排落位 → 输出
5. **交付三件套**：`xxx-overlay.json` + 段落表 + 素材清单。**除这三样外不输出解释文字**
6. **lint 自检（交付前必跑）**：
   ```bash
   cd /Users/mac/Documents/overlay-studio/motion-playground && \
   npm run -s lint:overlay -- "<json绝对路径>" --duration <视频秒数>
   ```
   error 必须修完再交付；warn 随交付附给用户过目
7. **落盘位置**：JSON 存**和 SRT 同一个素材目录**（一并维护该目录的 `STATUS.md`：开工先读、收工写回）

## 最容易忘的硬约束（详情以真源为准）

- **只能用卡片库里的 kind**（20 张），严禁发明新类型；`params` 不臆造 x/y/fontSize/十六进制色
- 强调色只有 `blue` / `alert` / `orange` 三个
- `start/end` **必须来自 SRT 真实时间**，end > start
- 是「**用动画呈现语义**」，不是「给字幕加特效」——卡的内容来自论点/证据，不是 SRT 原句复述
- `"lintOff"` 是用户亲手点的忽略，**重排/重写时原样保留，严禁"修复"回去**
- 交付时提醒**抠像**（entity-chips / focus-card 会盖脸）

## 与仓库侧的关系

- 在仓库根目录开会话 → 走原生 skill（更顺，lint 相对路径可用）
- **在 vault 开会话 → 走本入口**（绝对路径调 lint，等价）
- 更新：`cd /Users/mac/Documents/overlay-studio && git pull`（会覆盖 skill 文件，个人规则写进 `我的偏好.md`，别改 SKILL.md）

## 边界

只管「成片前加动效层」这一段。**不替代**选题 / 脚本 / 合规 / 发布 —— 口播稿仍走 [[B自媒体大脑/01-视频号创作/创作脚本规则/视频号脚本风格系统|视频号脚本系统]]，发布前仍过 [[F技能库skill/运行规则/01-合规网关|01-合规网关]]。
