# 07 · 脚本库打包 — 用户说"整理脚本库/打包脚本/整合成docx"时执行

用户说"整理脚本库"/"打包脚本"/"整合成文档"/"打包成 docx"等 → 自动执行：

① 收集指定脚本（安利选题脚本库 / AI选题脚本库 / 其他）
② 加上输出成品中的相关文稿（如 我的安利十年 等）
③ 打包成 .docx 文件
④ 输出到 → `04-Archive（归档历史）/03-短视频成片/`

**固定输出路径：** `04-Archive（归档历史）/03-短视频成片/` · **固定命名风格：** `主题·短视频脚本全集.docx`

| 你说 | 我自动执行 |
|------|-----------|
| **`整理脚本库` / `打包脚本` / `整合成docx`** 及类似 | 收集指定脚本库 → 读取相关文稿 → 生成 .docx → 输出到 `04-Archive（归档历史）/03-短视频成片/` |
| **爆款了 / 这个数据不错 / 帮我分析这个数据** 及类似 | 读取对应脚本数据 → 分析爆款原因 → 记录到 `00-灵感库（标记灵感）/爆款视频/` → 更新操作系统总控台状态 |
